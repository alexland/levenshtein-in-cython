language: python
python:
  - 3.4
install:
  - pip install -r requirements.txt --use-mirrors
  - pip install python-coveralls
  - pip install nose
script:
  - python3 runtests.py
  - nosetests --with-coverage
after_success:
  - coveralls
